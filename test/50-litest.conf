# Ignore devices created by libinput's test suite (litest)
Section "InputClass"
        Identifier "libinput test suite blacklist"
        MatchProduct "litest"
        Option "Ignore" "on"
EndSection
